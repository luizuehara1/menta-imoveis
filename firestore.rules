rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Permit read and write on all collections for full functionality
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
